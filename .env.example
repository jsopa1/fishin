# No credentials required for current phase (V0 - Prediction Feasibility)
# 
# When integrating external APIs (weather, fishing data, etc.),
# add API keys here. Never commit .env with real credentials.
# 
# Example (when needed):
# WEATHER_API_KEY=your-api-key-here
# GITHUB_TOKEN=your-github-token-here

