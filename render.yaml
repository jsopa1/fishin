services:
  - type: web
    name: fishin-v1-review
    runtime: python
    plan: free
    buildCommand: pip install -r webapp/requirements.txt
    startCommand: gunicorn --chdir webapp app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
